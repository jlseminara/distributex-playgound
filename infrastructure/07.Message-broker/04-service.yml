apiVersion: v1
kind: Service
metadata:
  name: rabbit-cluster-local
spec:
  type: LoadBalancer
  selector:
    cnpg.io/cluster: rabbit-cluster
    role: primary
  ports:
  - port: 15672
    targetPort: 15672
